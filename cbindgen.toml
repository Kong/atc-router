language = "C"
header = "/* Generated by cbindgen.  Do NOT edit. */"
documentation = false
no_includes = true

[enum]
prefix_with_name = false

[defines]
# bindings only make sense if ffi is enabled
# "feature = ffi" = "DEFINE_ATC_ROUTER_FFI"

[macro_expansion]
bitflags = true

[export]
include = [
    "BinaryOperatorFlags",
    "ATC_ROUTER_EXPRESSION_VALIDATE_OK", 
    "ATC_ROUTER_EXPRESSION_VALIDATE_FAILED",
    "ATC_ROUTER_EXPRESSION_VALIDATE_BUF_TOO_SMALL"
]

[export.rename]
# Cbindgen mangles generics, but for ffi, there's only one router type
"Router______Schema" = "Router"